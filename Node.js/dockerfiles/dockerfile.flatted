# Use official Node.js 22 image
FROM node:22

WORKDIR /workdir

RUN apt-get update && apt-get install -y \
    curl \
    nano \
    git \
    ca-certificates \
    xz-utils \
    && rm -rf /var/lib/apt/lists/*

# Initialize npm project
RUN npm init -y

# Install brace-expansion
RUN npm install flatted@3.3.4

CMD ["/bin/bash"]